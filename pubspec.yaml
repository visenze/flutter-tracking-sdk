name: visenze_tracking_sdk
description: Flutter plugin for tracking for ViSenze console users. Created for Flutter iOS, Android and Web apps.
version: 0.0.3
homepage: https://visenze.com
repository: https://github.com/visenze/flutter-tracking-sdk/
documentation: https://github.com/visenze/flutter-tracking-sdk/
issue_tracker: https://github.com/visenze/flutter-tracking-sdk/issues

environment:
  sdk: '>=2.19.4 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5
  shared_preferences: "^2.0.18"
  uuid: ^3.0.6
  device_info_plus: ^8.1.0
  clock: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  fake_async: ^1.3.1


flutter:
